// Shared enum types for ProductGrid and its components.

enum ProductGridStyle { card, flat, elevated, outlined }

enum ProductGridLayout { grid, list, staggered }

enum ProductGridImageStyle { cover, contain }

enum ProductGridAnimationType { none, fadeIn, slideUp, scale }
